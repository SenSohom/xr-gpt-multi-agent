# Web stack
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# Vision / models
numpy>=1.24
pillow>=10.0
ultralytics>=8.1            # YOLOv8
transformers>=4.44,<5.0     # FastVLM remote-code model loader
einops>=0.7
timm>=0.9
accelerate>=0.27
sentencepiece>=0.1.99
huggingface_hub>=0.20

# torch is intentionally NOT pinned here. Install the CUDA build manually:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# (or CPU build:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu )
#
# IMPORTANT: install torch BEFORE running `pip install -r requirements.txt`,
# AND re-run the torch install AFTERWARDS — ultralytics' transitive dep on `torch`
# without the CUDA index URL will silently downgrade you to CPU torch.
